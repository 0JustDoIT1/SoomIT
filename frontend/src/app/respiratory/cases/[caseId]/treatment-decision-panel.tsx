"use client";
import { useEffect, useState } from "react";
import type { AuthorizedFetch, RegimenCandidate, TreatmentDecision } from "./treatment-prescription-types";
import { TreatmentEvidencePanel } from "./treatment-evidence-panel";
import { TreatmentOpinionPanel } from "./treatment-opinion-panel";
export function TreatmentDecisionPanel({ caseId, apiBaseUrl, authorizedFetch, onTreatmentChanged }: { caseId: string; apiBaseUrl: string; authorizedFetch: AuthorizedFetch; onTreatmentChanged?: (decision: TreatmentDecision) => void }) { const [candidates, setCandidates] = useState<RegimenCandidate[]>([]); const [decision, setDecision] = useState<TreatmentDecision | null>(null); const [selected, setSelected] = useState(""); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const load = async () => { try { setLoading(true); const [a,b] = await Promise.all([authorizedFetch(`${apiBaseUrl}/api/doctor/cases/${caseId}/regimen-candidates/`), authorizedFetch(`${apiBaseUrl}/api/doctor/cases/${caseId}/treatment-decision/`)]); if (!a.ok) throw new Error("Regimen candidates request failed."); const cs = await a.json(); setCandidates(cs); const d = b.ok ? await b.json() : null; setDecision(d); setSelected(d?.selected_regimen ?? ""); } catch (e) { setError(e instanceof Error ? e.message : "Treatment decision request failed."); } finally { setLoading(false); } }; useEffect(() => { void load(); }, [caseId]); const save = async (confirm = false) => { setBusy(true); setError(""); try { const r = await authorizedFetch(`${apiBaseUrl}/api/doctor/cases/${caseId}/treatment-decision/${confirm ? "confirm/" : ""}`, { method: confirm ? "POST" : "POST", headers: { "Content-Type": "application/json" }, body: confirm ? undefined : JSON.stringify({ selected_regimen: selected }) }); const d = await r.json().catch(() => ({})); if (!r.ok) throw new Error(d.detail || "Treatment decision request failed."); setDecision(d); onTreatmentChanged?.(d); } catch (e) { setError(e instanceof Error ? e.message : "Treatment decision request failed."); } finally { setBusy(false); } }; if (loading) return <section className="rounded-lg bg-white p-4 text-sm">치료 후보 조회 중...</section>; return <div className="space-y-4"><section className="rounded-lg border bg-white p-4"><h2 className="text-sm font-bold">Treatment Decision</h2>{error && <p role="alert" className="mt-2 text-xs text-rose-600">{error}</p>}{candidates.length === 0 ? <p className="mt-3 text-xs text-slate-500">현재 치료 후보가 없습니다.</p> : <div className="mt-3 space-y-2">{candidates.map((c) => <button type="button" key={c.id} onClick={() => setSelected(c.regimen_detail.id)} className={`block w-full rounded border p-3 text-left ${selected === c.regimen_detail.id ? "border-blue-500 bg-blue-50" : "border-slate-200"}`}><p className="text-sm font-semibold">{c.regimen_detail.regimen_name} ({c.regimen_detail.regimen_code})</p><p className="text-xs">Rule {c.rule_code} · priority {c.priority}</p><p className="text-xs text-slate-500">{c.match_reasons.join(" · ")}</p><p className="text-xs text-slate-500">{c.evidence_source ?? "-"}</p></button>)}</div>}<div className="mt-3 flex gap-2"><button type="button" disabled={!selected || busy} onClick={() => void save()} className="rounded bg-blue-600 px-3 py-2 text-xs text-white disabled:bg-slate-300">DRAFT 저장</button><button type="button" disabled={!decision || busy} onClick={() => void save(true)} className="rounded bg-emerald-600 px-3 py-2 text-xs text-white disabled:bg-slate-300">확정</button></div><p className="mt-2 text-xs text-slate-500">현재 선택: {selected || "없음"} · 상태: {decision?.decision_status ?? "-"}</p></section><TreatmentEvidencePanel caseId={caseId} apiBaseUrl={apiBaseUrl} authorizedFetch={authorizedFetch} /><TreatmentOpinionPanel caseId={caseId} apiBaseUrl={apiBaseUrl} authorizedFetch={authorizedFetch} /></div>; }
