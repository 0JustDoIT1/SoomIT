# Cloud Run runtime environment contract.
HF_HOME=/root/.cache/huggingface
HF_HUB_OFFLINE=1
VLLM_USE_V1=0
PORT=8000

# Docker BuildKit secret required only while building the image.
HF_TOKEN=change-me
